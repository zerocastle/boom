<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">

	<select id="getTime" resultType="string">
		SELECT sysdate FROM DUAL
	</select>
	
	<insert id="memberRegister" parameterType="hashmap">
	INSERT INTO MEMBER VALUES
	(#{nickname},#{phone},#{m_password},#{email},#{manner},#{intro},#{warning},#{img_path},#{partner_signal},#{acc_num});
	</insert>
	
	<select id="memberLogin" resultType="MemberVO">
	SELECT * FROM MEMBER WHERE PHONE=#{phone} 
	AND M_PASSWORD=#{m_password}
	</select>


</mapper>